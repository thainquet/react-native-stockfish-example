# react-native-stockfish-example
Use ```react-native-static-server``` for serving stockfishJS.

Communicate betweet native code and JS worker by webview function: ```onMessage``` and ```postMessage```.
